// iniciar el servicio con Docker

    cd Documentos/agente-ventas

    docker compose up --build

    # luego abre http://localhost:8501 en el navegador

    # IMPORTANTE: antes de iniciar la llamada, completa la "Vapi Public API Key" en la sección de credenciales dentro de la app.

    